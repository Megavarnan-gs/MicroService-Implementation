package com.regulareedge.notificationservice.common.enums;

/**
 * Mirrors com.regularedge.enums.NotificationCategory from the monolith so category
 * values stay consistent across the platform.
 */
public enum NotificationCategory {
    OBLIGATION,
    DATA_COLLECTION,
    RETURN,
    CORRESPONDENCE,
    PENALTY,
    AUDIT
}
